/// Parsed API payload for basic-profile calls (extend when API is defined).
class UserBasicProfileResponseModel {
  const UserBasicProfileResponseModel();

  factory UserBasicProfileResponseModel.fromJson(Map<String, dynamic> _) {
    return const UserBasicProfileResponseModel();
  }
}

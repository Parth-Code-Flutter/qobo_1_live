/// Request body for update profile API — replace fields when contract is defined.
class UpdateProfileRequestModel {
  const UpdateProfileRequestModel();

  Map<String, dynamic> toJson() => <String, dynamic>{};
}
